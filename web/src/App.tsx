/**
 * Annapurna web — M0 placeholder shell.
 *
 * The real screens (features dashboard, feature drill-down, onboarding wizard)
 * are built in M2/M6. This is just enough to prove the React + TypeScript build
 * and test runner are wired up.
 */
export function App() {
  return (
    <main>
      <h1>Annapurna</h1>
      <p>Per-feature AI cost attribution — build vs. inference, never blended.</p>
    </main>
  );
}
